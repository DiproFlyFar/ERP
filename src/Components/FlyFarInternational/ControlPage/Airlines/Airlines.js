import { Box } from "@mui/material";
import React from "react";

const Airlines = () => {
  return (
    <Box
      sx={{
        display: "flex",
        alignItems: "center",
        mt: "20px",
        flexWrap: "wrap",
        width: "100%",
      }}
    >
      airlines
    </Box>
  );
};

export default Airlines;
